version: "3.8"

services:
  react-app:
    build: .
    image: compro_fba_fe
    container_name: compro_fba_fe
    ports:
      - "5173:5173"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    environment:
      - REACT_APP_API_URL=http://localhost:3000